# Built-in dash panel EDID — see linux-bb.org_%.bbappend. The first three
# entries are bb.org_defconfig's own and must stay: this string replaces it.
CONFIG_DRM_LOAD_EDID_FIRMWARE=y
CONFIG_EXTRA_FIRMWARE="regulatory.db regulatory.db.p7s cadence/mhdp8546.bin edid/waveshare-104-1600x720.edid"
CONFIG_EXTRA_FIRMWARE_DIR="firmware"
